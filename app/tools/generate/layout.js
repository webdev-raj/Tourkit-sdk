export const metadata = {
  title: 'Free AI Tour Generator — TourKit',
  description:
    'Generate onboarding tour steps for any website in seconds using AI. Free, no signup required.',
}

export default function GenerateToolLayout({ children }) {
  return children
}
